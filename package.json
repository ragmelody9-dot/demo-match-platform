{
  "name": "demo-match-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Responsive demo-only task/match platform inspired by the supplied screenshot.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}